name: mini_todo_app
description: A mini app built with ui_eval - type-safe UI DSL
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ui_eval:
    path: ../../core

dev_dependencies:
  lints: ^3.0.0
