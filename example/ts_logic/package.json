{
  "name": "example-logic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && node esbuild.config.js",
    "watch": "tsc --watch & node esbuild.config.js --watch"
  },
  "dependencies": {
    "@ui_eval/sdk": "file:../../ts_sdk"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "esbuild": "^0.19.0"
  }
}
