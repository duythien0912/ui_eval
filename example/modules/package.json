{
  "name": "ui_eval_example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:logic": "node esbuild.js",
    "build:logic:watch": "node esbuild.js --watch",
    "postinstall": "cd ../ts_sdk && npm install && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  }
}
