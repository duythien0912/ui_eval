name: ui_eval
description: Type-safe UI DSL for Flutter
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_js: 0.8.7
  http: ^1.1.0
  path_provider: ^2.1.1

  # Production-ready packages for stability and reduced complexity
  jinja: 0.6.5
  flutter_riverpod: 3.2.0
  riverpod: 3.2.0

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
